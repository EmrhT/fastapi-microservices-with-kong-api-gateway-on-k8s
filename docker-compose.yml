version: '3'

services:
  items:
    build:
      context: ./items
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./items:/app
    command: sh -c "uvicorn main:app --reload --host 0.0.0.0 --log-level trace"
